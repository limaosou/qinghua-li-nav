# 服务端口
PORT=3000
# 监听地址，服务器上保持 0.0.0.0
HOST=0.0.0.0

# ===== 管理员账号 =====
ADMIN_USERNAME=admin
# 推荐：scrypt 哈希密码（用 `npm run hash-password` 生成后粘贴到这里）
ADMIN_PASSWORD_HASH=
# 备选：明文密码（仅本地测试用，生产环境请用上面的哈希）
# ADMIN_PASSWORD=

# Token 签名密钥，务必改成一段长随机字符串
TOKEN_SECRET=please-change-me-to-a-long-random-string

# SQLite 数据文件目录（相对项目根目录，建议加入定时备份）
DATA_DIR=./data
